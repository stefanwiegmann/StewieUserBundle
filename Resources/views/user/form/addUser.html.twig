{{ form_start(form, {'attr': {'class': 'form-inline'} }) }}
    <div class="input-group">
        {{ form_widget(form.userAuto, {'attr': {'placeholder': 'label.startTyping'} }) }}
        {{ form_widget(form.submit) }}
    </div>
{{ form_end(form) }}
