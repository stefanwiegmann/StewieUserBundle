{% extends '@StefanwiegmannUser/default/layout.html.twig' %}
{% trans_default_domain 'SWUserBundle' %}

{% block main %}
<br>
<div class="row justify-content-md-center">
    <div class="col-md-6">
      <div class="container-fluid">
        <h1 class="h3 mb-3 font-weight-normal">{{ 'header.register.complete'|trans }}</h1>

      <!-- actual form -->
      {{ form_start(form) }}

          <div class="form-group row">
            <div class="col-sm-4">
              <strong>{{ 'label.email'|trans }}</strong>
            </div>
            <div class="col-sm-8">
              {{ user.email }}
            </div>
          </div>

          <div class="form-group row">
            <div class="col-sm-4 col-form-label">
              <strong>{{ form_label(form.username) }}</strong>
            </div>
            <div class="col-sm-8">
              {{ form_errors(form.username) }}
              {{ form_widget(form.username) }}
            </div>
          </div>

          <div class="form-group row">
            <div class="col-sm-4 col-form-label">
              <strong>{{ form_label(form.password.first) }}</strong>
            </div>
            <div class="col-sm-8">
              {{ form_errors(form.password.first) }}
              {{ form_widget(form.password.first) }}
            </div>
          </div>

          <div class="form-group row">
            <div class="col-sm-4 col-form-label">
              <strong>{{ form_label(form.password.second) }}</strong>
            </div>
            <div class="col-sm-8">
              {{ form_errors(form.password.second) }}
              {{ form_widget(form.password.second) }}
            </div>
          </div>

          <div class="form-group row">
            <div class="col-sm-4 col-form-label">
              <strong>{{ 'label.fullName'|trans }}</strong>
            </div>
            <div class="col-sm-auto">
              {{ form_errors(form.firstName) }}
              {{ form_widget(form.firstName, {'attr': {'placeholder': 'label.firstName'} }) }}
            </div>
            <div class="col-sm-auto">
              {{ form_errors(form.lastName) }}
              {{ form_widget(form.lastName, {'attr': {'placeholder': 'label.lastName'} }) }}
            </div>
          </div>

          <br>
          <div class="row justify-content-md-center">
            {{ form_widget(form.submit) }}
          </div>


      {{ form_end(form) }}
    </div>
  </div>
</div>
{% endblock main %}
